## cleanup
function for mode handling in fn lex

## features
decent error handling
builtins (cd!)
"Shell stuff" (pipes, streams, job control, etc...)
escape chars and other literals fun (make it legit)
comments
re-parse output
literals for s[] l[] d[]
functions
loops
scopes
set notation (s[x | x in ys, x > 2])
config (ps1, aliases, colors, etc.)

## crazy
compilation
jit
